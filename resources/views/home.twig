{% extends "templates/app.twig" %}
{% set title = 'Home' %}

{% block content %}
  <div class="container">
    <div class="row">
      {% for index, product in products %}
        <div class="col-md-4 col-sm-6">
          {% include 'products/partials/item.twig' %}
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}
